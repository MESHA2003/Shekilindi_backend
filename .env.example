# Django Settings
SECRET_KEY=your-super-secret-key-here-generate-one-for-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,127.0.0.1:8000

# CORS Settings (comma-separated list of allowed origins)
# For development:
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000,http://localhost:5173

# Database (optional, leave empty to use SQLite for development)
# DATABASE_URL=postgresql://user:password@localhost:5432/herbal_clinic

# For production (Render)
# DEBUG=False
# SECRET_KEY=<generate-with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())">
# ALLOWED_HOSTS=herbal-clinic-backend.onrender.com
# CORS_ALLOWED_ORIGINS=https://shekilindi-herbal.vercel.app,https://herbmedi-realy-back.vercel.app
# DATABASE_URL=<provided-by-render>
